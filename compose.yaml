services:
  web:
    image: lempake-web-profile-web:latest
    build: .
    ports:
      - "8181:80"
    environment:
      - APP_NAME
      - APP_KEY
      - APP_ENV
      - APP_URL
      - DB_CONNECTION
      - DB_HOST
      - DB_PORT
      - DB_DATABASE
      - DB_USERNAME
      - DB_PASSWORD